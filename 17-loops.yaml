- name: install multiple packages in ansible
  hosts: frontend
  become: yes
  tasks:
  - name: install all packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
      - nginx
      - mysql
      - git